pragma solidity ^0.5.0;

contract OneSmallStep {
    
    
    //Create function quote() that returns "That's one small step for man, one giant leap for mankind."
    function quote() public pure returns (string memory) {
        return "That's one small step for man, one giant leap for mankind.";
    }
    
}
